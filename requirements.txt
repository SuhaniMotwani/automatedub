# ============================================================
# torch (CUDA-enabled) — install separately, NOT via this file
# ============================================================
# Installed and verified working: torch==2.14.0+cu130
# (replace with your actual version/tag from: pip freeze | Select-String torch)
#
# To install or reinstall torch with GPU support, run this FIRST,
# before installing the rest of this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
#
# Do NOT add a plain "torch" line below — a plain `pip install -r
# requirements.txt` would pull the CPU-only build from PyPI's default
# index and silently break GPU support.
# ============================================================

# Everything below is installed normally via:
#   pip install -r requirements.txt
# Antigravity will add entries here as each module is scaffolded.
yt-dlp>=2025.1.15
faster-whisper>=1.2.0
edge-tts>=7.0.0
librosa>=0.10.0